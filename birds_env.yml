name: model
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - segmentation-models
    - tensorflow==2.5.0
    - matplotlib
    - pillow
    - tqdm
    - labelbox
    - ipython
    - ipykernel 